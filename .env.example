# Core server
PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/RojGar

# Auth / JWT
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRE=7d

# Rate limiting (optional, already used)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# OTP config
OTP_EXPIRE_MINUTES=15
OTP_MAX_ATTEMPTS=5

# Fast2SMS Configuration
# Get your API key from: https://www.fast2sms.com/
FAST2SMS_API_KEY=your_fast2sms_api_key_here
# Route options: 'q' (Quick SMS - no verification), 'otp' (requires verification), 'dlt' (requires DLT registration)
FAST2SMS_ROUTE=q
# Message format: 'simple' (shorter, better delivery) or 'detailed' (default)
FAST2SMS_MESSAGE_FORMAT=simple

# Firebase Cloud Messaging (FCM)
# Option 1: Use service account file (recommended for development)
FIREBASE_SERVICE_ACCOUNT=./firebase-service-account.json

# Option 2: Use environment variables (recommended for production)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYourPrivateKeyHere\n-----END PRIVATE KEY-----\n"
